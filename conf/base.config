
process {
    cpus   = { 1    * task.attempt }
    memory = { 4.GB * task.attempt }
    time   = { 1.h  * task.attempt }

    errorStrategy = { task.exitStatus in [143,137,104,134,139,5,6,null] ? 'retry' : 'finish' }
    maxRetries    = 3
    maxErrors     = '-1'

    // Process-specific resource requirements. Requests are capped to each
    // profile's `process.resourceLimits` (set in nextflow.config), so labels can
    // request generously without exceeding the executor's real limits.
    withLabel:process_low_constant {
        cpus   = 2
        memory = 8.GB
        time   = 1.h
    }
    withLabel:process_low {
        cpus   = { 2     * task.attempt }
        memory = { 8.GB  * task.attempt }
        time   = { 2.h   * task.attempt }
    }
    withLabel:process_medium {
        cpus   = { 4     * task.attempt }
        memory = { 15.GB * task.attempt }
        time   = { 8.h   * task.attempt }
    }
    withLabel:process_high {
        cpus   = { 32    * task.attempt }
        memory = { 60.GB * task.attempt }
        time   = { 8.h   * task.attempt }
    }
    withLabel:process_long {
        time   = { 20.h  * task.attempt }
    }
    withLabel:process_high_memory {
        memory = { 40.GB * task.attempt }
    }
    withLabel:error_ignore {
        errorStrategy = 'ignore'
    }
    withLabel:error_retry {
        errorStrategy = 'retry'
        maxRetries    = 2
    }

    // labels added for TEI-REX workflows
    withLabel:process_high_constant {
        cpus   = 128
        memory = 60.GB
        time   = 8.h
    }

    withLabel:process_very_long {
        time   = { 240.h * task.attempt }
    }
}
